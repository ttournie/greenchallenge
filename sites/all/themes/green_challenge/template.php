<?php


/**
 * Override or insert variables into the page template.
 */
function green_challenge_process_page(&$variables) {
  //Hide the page title
  dsm($variables);
    
   //$variables['title'] = NULL;
 
}
